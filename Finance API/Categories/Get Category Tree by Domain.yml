info:
  name: Get Category Tree by Domain
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/v1/categories/tree?domain=finance"
  params:
    - name: domain
      value: finance
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
