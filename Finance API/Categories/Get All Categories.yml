info:
  name: Get All Categories
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/categories"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
