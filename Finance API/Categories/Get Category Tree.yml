info:
  name: Get Category Tree
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/categories/tree"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
