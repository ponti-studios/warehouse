info:
  name: Delete Account
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/accounts/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
