info:
  name: Get Account by ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/accounts/:id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
