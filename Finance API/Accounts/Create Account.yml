info:
  name: Create Account
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/v1/accounts"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "name": "Test Checking",
          "type": "CHECKING",
          "currency": "USD"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
