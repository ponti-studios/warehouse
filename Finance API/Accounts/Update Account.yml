info:
  name: Update Account
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/api/v1/accounts/:id"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "name": "Updated Account Name"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
